import { parseJson, respond } from "@/app/api/v1/_lib/api-response"; import { getLaboratoryService } from "@/laboratory/runtime"; import { laboratorySessionResponseSchema, resetLaboratorySchema } from "@/laboratory/schemas";
export async function POST(request: Request, { params }: { params: Promise<{ sessionId: string }> }): Promise<Response> { const { sessionId } = await params; return respond(laboratorySessionResponseSchema, async () => { const input = await parseJson(request, resetLaboratorySchema); return { session: getLaboratoryService().reset(sessionId, input.presetId) }; }); }

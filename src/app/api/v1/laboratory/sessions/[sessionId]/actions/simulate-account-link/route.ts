import { parseJson, respond } from "@/app/api/v1/_lib/api-response"; import { laboratorySessionResponseSchema, simulateAccountLinkSchema } from "@/laboratory/schemas"; import { requireApiUser } from "@/lib/auth/current-user"; import { mutateLaboratorySession } from "@/persistence/laboratory-session-store";
export async function POST(request: Request, { params }: { params: Promise<{ sessionId: string }> }): Promise<Response> { const { sessionId } = await params; return respond(laboratorySessionResponseSchema, async () => { const input = await parseJson(request, simulateAccountLinkSchema); const user = await requireApiUser(); return { session: await mutateLaboratorySession(user.id, sessionId, (service) => service.simulateAccountLink(sessionId, input)) }; }); }

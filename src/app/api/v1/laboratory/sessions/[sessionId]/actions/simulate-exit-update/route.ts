import { parseJson, respond } from "@/app/api/v1/_lib/api-response"; import { getLaboratoryService } from "@/laboratory/runtime"; import { laboratorySessionResponseSchema, simulateExitSchema } from "@/laboratory/schemas";
export async function POST(request: Request, { params }: { params: Promise<{ sessionId: string }> }): Promise<Response> { const { sessionId } = await params; return respond(laboratorySessionResponseSchema, async () => { const input = await parseJson(request, simulateExitSchema); return { session: getLaboratoryService().simulateExit(sessionId, input) }; }); }
